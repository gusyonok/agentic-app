MODEL_PROVIDER=openai
MODEL_NAME=gpt-4o-mini
OPENAI_API_KEY=your_openai_api_key_here
# Optional: monetary suffix in reports (e.g. UAH). Leave empty to use neutral "same units as input".
REPORT_CURRENCY_SUFFIX=
